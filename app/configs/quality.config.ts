export const MIN_QUALITY = 0;
export const MAX_QUALITY = 50;
export const LEGENDARY_QUALITY = 80;

export const DEFAULT_QUALITY_BOUNDS = {
  min: MIN_QUALITY,
  max: MAX_QUALITY,
};

export const LEGENDARY_QUALITY_BOUNDS = {
  min: LEGENDARY_QUALITY,
  max: LEGENDARY_QUALITY,
};
